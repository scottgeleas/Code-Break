{
  "name": "code-break",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/server.js",
    "develop": "concurrently \"cd server && npm run watch\" \"cd code-break && npm start\"",
    "install": "cd server && npm i && cd ../code-break && npm i",
    "build": "cd code-break && npm run build",
    "lint": "./node_modules/.bin/eslint --format codeframe "
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^6.2.0",
    "eslint": "^7.32.0"
  },
  "dependencies": {
    "graphql": "^15.5.1"
  }
}
